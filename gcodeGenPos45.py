x_len = input('Input length of specimen(mm):')
y_len = input('Input width of specimen(mm): ')
gap = int(input('Input gap between lines(mm): '))
speed = input('Input speed(mm/min): ')

#calculate the x point
dis = gap * (1.4)




input("Press enter to exit")

